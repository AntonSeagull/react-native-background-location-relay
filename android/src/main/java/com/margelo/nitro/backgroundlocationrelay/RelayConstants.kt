package com.margelo.nitro.backgroundlocationrelay

object RelayConstants {
  // Must escape $ in Kotlin string literals — "$GEO$" is treated as interpolation.
  const val GEO_PLACEHOLDER = "\$GEO\$"
}
